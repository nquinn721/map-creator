.mode-menu(ng-controller="ModeMenu as mm")
	| {{mm.title}}
	.modes
		div(ng-attr-title="{{mode.name.capitalize()}} [{{mode.shortcut}}]", ng-repeat="mode in mm.stage.modes", ng-class="{selected: mm.stage.mode == mode.name}", ng-click="mm.stage.setMode(mode.name)")
			i(ng-class="m.getFAClass(mode)")